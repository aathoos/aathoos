<UserControl
  x:Class="aathoos.Views.TasksPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="{StaticResource AppBackgroundBrush}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="1" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Grid Grid.Row="0" Margin="32,32,32,20">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock Text="Tasks" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource AppForegroundBrush}" />
        <TextBlock Text="Deadlines, priorities, progress" FontSize="14"
                   Foreground="{StaticResource AppMutedBrush}" Margin="0,4,0,0" />
      </StackPanel>

      <Button x:Name="AddButton" Grid.Column="1"
              Click="OnAddClick"
              Background="{StaticResource AppSurfaceBrush}"
              Foreground="{StaticResource AppForegroundBrush}"
              BorderThickness="0" Padding="14,8"
              VerticalAlignment="Center">
        <TextBlock Text="+" FontSize="20" FontWeight="Bold" />
      </Button>
    </Grid>

    <Border Grid.Row="1" Background="{StaticResource AppBorderBrush}" />

    <!-- Task list -->
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <StackPanel x:Name="TaskRows" Margin="0,8,0,8" />
    </ScrollViewer>
  </Grid>
</UserControl>
